import { RunningTimePipe } from './running-time.pipe';

describe('RunningTimePipe', () => {
  it('create an instance', () => {
    const pipe = new RunningTimePipe();
    expect(pipe).toBeTruthy();
  });
});
